# 质量保障机制 · 已建立

## 为什么老是犯错

回顾这几轮，我犯的错有一个共同点：**说了但没做，做完没验证**。

| 错误 | 本质 |
|---|---|
| 说环境编译不了 APK，其实能 | 没验证就下结论 |
| 文档写「我的 → 内容更新」，界面根本没做 | 只写文档不建界面 |
| 打包脚本替换失效，代码缺一大半 | 改完没检查产物 |
| 让概率跟着市场覆盖，分歧归零 | 没做数值合理性检查 |
| 让球盘混进胜平负共识，偏 20pt | 同上 |

不是能力问题，是**流程缺一环**。现在把这环补上。

---

## 一、交付前自检（已建，每次必跑）

```
python3 tools/selfcheck.py          # 常规
python3 tools/selfcheck.py --strict # 正式交付，警告也要清零
```

**8 项检查**：

1. 全部 JS 语法
2. **app.js 引用的元素 ID，HTML 里必须存在** ← 专治「说了没做」
3. HTML 的交互元素，app.js 必须处理
4. **文档承诺的功能，产物里必须真实存在** ← 专治「文档写了没做」
5. 引擎数值回归（72 场，概率和=1、无 NaN、赔率>1.02）
6. 官方数据解析链路（用真实结构样本）
7. 构建产物完整性（必须完全自包含）
8. APK 内容、签名、无第三方链接

### 已验证：它真能抓错

我故意把「内容更新」卡片删掉（复现本次的错误），再跑自检：

```
❌ app.js 引用了 HTML 中不存在的元素  #btnCheckUpdate
❌ app.js 引用了 HTML 中不存在的元素  #btnResetBuiltin
❌ app.js 引用了 HTML 中不存在的元素  #btnSaveUpdate
❌ app.js 引用了 HTML 中不存在的元素  #setUpdateOn
❌ app.js 引用了 HTML 中不存在的元素  #setUpdateUrl
❌ app.js 引用了 HTML 中不存在的元素  #updateCard
❌ app.js 引用了 HTML 中不存在的元素  #updateStatus
通过 30 ｜ 警告 0 ｜ 失败 7
```

**7 处全部抓出**。恢复后再跑：全绿。

当前状态：**通过 31 ｜ 警告 0 ｜ 失败 0**。

---

## 二、每天定时检查（已建）

| 任务 | 时间 |
|---|---|
| 竞彩数据每日自检 | **每天 09:00** |
| 竞彩智析周一版本更新 | **每周一 07:00** |

### 每日 09:00 做什么

1. 跑全量自检
2. **抓竞彩官方当日赛程，验证接口字段没变**
   —— 这是最可能出问题的地方。官方要是改了字段名（比如 `subMatchList` 改名），解析就全废了，而表面看不出来
3. 引擎数值回归
4. 异常就地修复 → 重新自检到全绿 → 推送 GitHub Pages
5. 简报给你（正常也会说一句）

### 每周一 07:00 做什么

1. `--strict` 自检（零失败零警告）
2. 确认官方接口结构未变
3. **全量重建 APK** + 校验签名
4. 全部通过才发布：递增版本号 → 部署 GitHub Pages → 创建版本标签
5. 把版本号和变更点发你

**任一环节没过就中止发布**，保持当前版本不变——不会带着问题上线。

> 每日检查我默认设的 09:00，你要是想要别的点告我改。周一的 07:00 是你指定的。

---

## 三、我给自己加的规矩

以后每一步：

1. **做完先跑自检**，不全绿不发给你
2. **文档提到的功能，必须有对应代码**，自检脚本会查
3. **数值类改动必做合理性检查**——不能出现「概率和≠1」「赔率<1」这种
4. **下结论前先验证**——不再说「环境编译不了」这种没试过的话

---

## 现在的状态

```
✅ 自检全绿：通过 31 ｜ 警告 0 ｜ 失败 0
✅ 每日 09:00 自检已排期
✅ 每周一 07:00 版本更新已排期
```

装 APK → 填链接：
```
https://ronald9527.github.io/jc-football/
```

**还是那件事：装完把状态栏那行字发我**，我确认下官方直连在你那儿通不通。
